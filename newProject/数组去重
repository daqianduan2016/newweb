<script>
    var arr = [1,1,'12',null,null,undefined,undefined,NaN,NaN];

    console.log(new Set(arr)) // {1, "12", null, undefined, NaN}

    //set返回的是一个对象，Array.from可将对象转化为数组数组

    var newArr = Array.from(new Set(arr));

    console.log(newArr); //[1, "12", null, undefined, NaN]
    
</script>